import React, { useState } from "react"

const TemperatureLineGraph = props => {
  const [selectedCountry, setSelectedCountry] = useState(props)
  return <div></div>
}

export default TemperatureLineGraph
